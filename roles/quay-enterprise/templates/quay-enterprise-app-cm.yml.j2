apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ quay_enterprise_app_configmap }}"
  namespace: "{{ quay_enterprise_namespace }}"
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
