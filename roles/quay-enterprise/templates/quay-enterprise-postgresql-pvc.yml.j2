apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ quay_enterprise_postgres_claim_name }}"
  namespace: "{{ quay_enterprise_namespace }}"
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: "{{ quay_enterprise_postgres_claim_size }}"